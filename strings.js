let str = 'a';
str;

str = "a";
str;

str = String({a: 1});
str;

// template lterals
str = `a`;
str;
str = `a${1 + 1}`
str;
str = `a
b`;
str;
str = escape `<a href="what"></a>`
str;

str = 'abc'.charAt(1)
str

str = 'abc'[1]
str

